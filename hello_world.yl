func add(a, b) {
    return a + b;
}

func main() {
    println("Enter first variable: ");

    x = to_int(input());

    println("Enter second variable: ");

    y = to_int(input());

    z = add(x, y);

    println("Answer: ", z);
}